<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:paddingTop="30dip"
    android:stretchColumns="1" >

    <TableRow>
        <TextView android:text="@string/id_asignatura"/>
        <EditText
            android:id="@+id/editId_Asignatura"
            android:inputType="text"
            android:maxLength="7" />
    </TableRow>

    <TableRow>
        <TextView android:text="@string/nombre_asignatura" />
        <EditText
            android:id="@+id/editNombre_Asignatura"
            android:inputType="text"
            android:maxLength="30" />
    </TableRow>

    <TableRow>
        <TextView android:text="@string/Opciones" />
        <TableRow>

        </TableRow>
        <Button
            android:onClick="insertarAsignatura"
            android:text="@string/Insertar" />
        <Button
            android:onClick="limpiarTexto"
            android:text="@string/Limpiar" />
    </TableRow>
</TableLayout>